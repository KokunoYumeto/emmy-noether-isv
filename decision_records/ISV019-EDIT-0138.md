# ISV019-EDIT-0138 — active ED0015 Paper 45 notation mirror

Recorded: 2026-08-15 (Europe/Berlin)  
Status: applied and independently verified  
Scope: `source_latin/45-isv.tex` only; no producer, build, release, DOI, or publication mutation

## Decision

Mirror the three source-fidelity corrections made authoritative by the activated German ED0015 Paper 45 text:

- line 479, column 15: `d(y)` → `d_{i}(y)`;
- line 501, column 34: `d(y)` → `d_{i}(y)`;
- line 512, column 4: `f(0,y)` → `f_{i}(0,y)`.

The direct Interslavic component changed from 25,544 bytes / `404BFCAF848FFF7262CC0A55262B6740DCD597E86AC21FE590C3034472FED19A` to 25,556 bytes / `3C157B7B26092EFF27EDBCDFF2DB372022E467A7C7468D47621E7B9DA95E3542`. Exact positional reversal of those three sites restores the predecessor bytes. All four outside segments were copied unchanged and separately hashed by the applicator.

## Authority and dispositions

The active German authority is NOETH-DE-ED-0015 under pointer NOETH-DE-AUTH-v052-20260815. The published source is byte-identical to the candidate (2,154,017 bytes / `51A25101C04877AE740989E72B2AD65A7A7E65B081077C4A518BF1737AD5B907`), with version DOI `10.5281/zenodo.21940320`, a 56-file anonymous readback, and a CURRENT-last activation receipt.

This closes `ISV019-OPEN-P45-ED0015-MIRROR-0001`. The two CHANGE dispositions—indexed `d_i(y)` and indexed `f_i(0,y)`—are applied. The two independently adjudicated KEEP readings remain byte-exact: uppercase `P` retains its context-dependent use, and the Bemerkung-II sequence retains `h_i y^(K)` before the proof concludes `lambda=(K)`. There are no semantic holds.

## Validation

- Strict UTF-8 without BOM, NFC, LF-only transport, terminal LF, and 611 logical lines were preserved.
- Label, reference, environment, footnote, TeX-control, math-delimiter, comment, and dollar streams were preserved; the brace-pair increase is exactly the three required subscripts.
- Post-edit census: unindexed `d(y)` 0; `d_{i}(y)` 5; unindexed `f(0,y)` 0; `f_{i}(0,y)` 1.
- Every existing active producer and the inactive Paper 24 shadow remained byte-identical. Paper 45 is a direct standalone component and has no producer twin.
- No build or publication was warranted for this three-token source-only checkpoint.

## Transaction incident and recovery evidence

The first production invocation, `python -B .\apply_isv_ed0015_p45_notation_fidelity_v019.py --apply`, exited 1 before installation because its final locked revalidation treated its own stage and rollback images as unexpected source-directory files (`RuntimeError: Source-directory file topology drift`). The source remained at the exact predecessor pin, and bounded checks found no lock, stage, rollback, or bytecode residue. The pre-fix applicator was 47,858 bytes / `F5E434D4…`.

The fix permits only the two invocation-owned stage and rollback paths during that final locked pass, requires the exact normal source set plus those paths, and requires both transients to be regular non-symlinks under the resolved `source_latin` directory. The patched applicator is 48,569 bytes / `9B62CEA3ABFA2FC96F823596641B89A3F539F223936B5BD61A95E1F9EBC489BF`; two dry runs passed.

An isolated full-root copy under the explicit Slavic parent then completed `--apply` with exit 0, produced the exact successor pin, and left an empty residue set. Its resolved path was checked before deletion; the test tree was removed and confirmed absent. The transcript and test tree were not retained, which is an evidence-retention limitation, not a source or code defect. The production retry then exited 0 and installed the exact successor; current transaction residue is zero.

## Continuation

Continue the strongest disjoint residual-language family from sealed EDIT-0138, then complete the four-source Latin freeze before deterministic Cyrillic projection and the next meaningful cumulative build.
