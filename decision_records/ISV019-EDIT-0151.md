# ISV019-EDIT-0151 — context-sensitive semantic resolution tranche

Status: the exact three-file source successor is installed and independently
accepted at the applied-not-sealed checkpoint. This record and
`seal_isv_edit0151_v019.py` are metadata candidates pending independent
pre-apply review. Authoritative metadata remains exactly sealed at
ISV019-EDIT-0150 until the sealer is separately authorized and run.

## Primary decision

Resolve the remaining live Polish/Czech surface forms `právě`, `działa`, and
`użyvanjem` by context rather than by global substitution. Eleven
visible-prose replacements are admitted on eleven lines. The active Paper 24
producer changes with its cumulative Unit 108 mirror, so the one corresponding
`Identity` receipt is refreshed as a twelfth primitive edit.

| File and line | Unit | Predecessor | Successor | Decision |
|---|---:|---|---|---|
| cumulative base 10,509 | 077 | `právě` | `prědtym` | previously treated case |
| cumulative base 10,578 | 077 | `právě` | `točno` | precisely by the Clebsch--Gordan expansion |
| cumulative base 12,909 | 095 | `právě` | `prědtym` | previously proved result |
| cumulative base 13,102 | 097 | `právě` | `prědtym` | previously proved theorem |
| cumulative base 15,704 | 108 | `działa` | `jest izpolnjeny` | passive “is fulfilled” predicate |
| active Paper 24 producer 495 | 108 | `działa` | `jest izpolnjeny` | exact active-producer mirror |
| cumulative base 15,220 | 108 | old producer `Identity` | 95,661 B / `3C343513…4578` | exact producer receipt refresh |
| cumulative base 16,511 | 118 | `użyvanjem` | `upotrěboju` | instrumental usage construction |
| cumulative base 23,044 | 214 | `právě` | `točno` | precisely the third reduction |
| cumulative base 23,088 | 214 | `právě` | `točno` | precisely for the cited reason |
| cumulative base 23,296 | 215 | `právě` | `točno` | precisely the norm theorem |
| Work 45 line 143 | P45 | `právě` | `točno` | precisely the primary-ideal concept |

The semantic split is deliberate. German ED0015 controls the senses of the
source expressions: temporal/anaphoric *eben behandelt* and *eben bewiesen*
receive `prědtym`; focus-identifying *gerade* or copular identification receives
`točno`. A blind `právě` replacement was rejected. The pinned official
dictionary snapshot supplies `prědtym`, scientific `tȯčno`, `izpȯlniti`, and
`upotrěba`; official grammar supplies the passive-participle construction;
four exact `upotrěboju` parallels and an `izpolnjenyh` parallel in the accepted
corpus constrain the inflection.

## Exact source transition

The input cohort is the exact sealed EDIT-0150 eleven-file cohort, whose
ordered manifest is 1,359 bytes / SHA-256
`938EB311D71CB2CA5226E24DEAED75DC90A0DE2873D1B7D6445ADAE5D7D9E3F0`.
The installed outputs are:

| Changed artifact | Bytes | SHA-256 |
|---|---:|---|
| `source_latin/base-papers1-43-isv.tex` | 1,892,324 | `8D8902B23C1BB2B421D5D56870369330498FDC83A8BFC1B746DEE8307C2E45B0` |
| `source_latin/45-isv.tex` | 26,013 | `5960587A6561D570BEA36B2CA18FB44AB1DAD022AFF24A103E5717006032BEC5` |
| active Paper 24 producer | 95,661 | `3C343513CC303C7B9875C114DF143232ED10B3D45B3052DEFE159BF27EE14578` |

The exact output-cohort manifest is 1,359 bytes /
`A565B09D51AF4E52E68F5836951D574C669CF95D1D1CA1F482173912595414E8`.
All other cohort files remain byte-exact. The inactive Paper 24 working shadow
remains 95,451 bytes /
`871CD6E83833631A63986D8801E1A122B433FE5AECF37AE3641CF407EE8A5C03`;
its one historical `działa` occurrence is not a live-edition target.

## Frozen applicator and production receipt

The frozen applicator is `apply_semantic_resolution_tranche_v019.py`, 68,040
bytes / `11A40EFB219833431B02FC90CE400E3F61C730617AF1FEAB25E5F78BCFD1C3BF`.
Its compact no-write plan was reproduced twice byte-identically at 26,771
bytes / `8F4488362697B2D3800FF248AF2415727ABE00F01EAE0D96111B21694FFEE8B6`.
Its compact self-test was reproduced twice byte-identically at 28,258 bytes /
`ECF961A7F67F3525BE62EAC7D737B69CA3F9C4DFD194B8AB92D4F46C262B12E7`,
with embedded receipt 1,383 bytes /
`42E2E9B59ACF15093824164F0DA07619A031CCD0DE9F2AB2BA66001AE77F2436`.
All four runs exited zero with empty stderr.

The separately authorized production command
`python -B apply_semantic_resolution_tranche_v019.py --apply --compact`
exited zero. Its raw UTF-8 compact JSON stdout, including terminal LF, is
26,731 bytes /
`C98934145FAD38B96A4C22CC167F9D1D3CE62387AB11B527A45C2724A627837B`.
It parsed as decision `ISV019-EDIT-0151`, status `APPLIED_NOT_SEALED`, and
`workspace_files_mutated=true`. Stderr was empty: 0 bytes /
`E3B0C44298FC1C149AFBF4C8996FB92427AE41E4649B934CA495991B7852B855`.
The raw operating-system temporary transcript was deleted only after hashing
and parsing; independent reconstruction reproduced the exact stdout bytes.

## Independent post-apply acceptance

Independent QA accepted the applied successor before metadata sealing. Its
sorted compact UTF-8 JSON guard array, including terminal LF, covers 27
explicit files and is 6,033 bytes /
`5C2A0017E7FAEBA1F209B3513B45C5C29B20948B811D24F18ABF87DBEF1243DB`.
The before and after guard arrays are identical.

The post-apply audit reversed all eleven lexical replacements and the Unit 108
`Identity` refresh to the exact EDIT-0150 inputs and replayed them to the live
successors. The canonical manifests are:

- line decisions: 4,354 bytes /
  `4BD929F161FFBF41BBBE5FE1AB2DE787F06FE1156BEC3FE5FB57FFA8E5F0A524`;
- lexical primitives: 3,732 bytes /
  `01E08536C5483D71B3A37E24265A8D830117B90B10F341348DA916789B1EBF9B`;
- Unit 108 identity refresh: 515 bytes /
  `DC1D211B084E0E1A5966E70F843A3E81D6E5B1AE8DC574EB26DE73A876DE246F`;
- three-file exact reverse: 852 bytes /
  `ACEF1BC709F4E7B1E62496ED19D61124823CE73D25076376F7B5CC0F12C32FBE`.

Protected mathematics, balanced foreign payloads, TeX controls and locators,
environments, and all unmanifested comments remain exact. The cumulative
source retains 219 ordered producer units and unchanged `Source` locators. The
changed body units are exactly 077, 095, 097, 108, 118, 214, and 215; the only
changed `Identity` is Unit 108. All six active producer/body relationships are
exact before and after. Source-transaction and Python bytecode residue are
zero.

## Projection census and retained hold

The pinned Cyrillic-v2 scanner run over the projected four-source successor is
deterministic and structurally clean, but deliberately blocked fail-closed:

- 1,108 blocked-union occurrences;
- 502 unsupported occurrences;
- 704 Roman-identity holds;
- zero parse errors, coverage failures, and unknown argument commands.

The unsupported and Roman-identity categories overlap and are not additive.
The compact scanner receipt is 3,948 bytes /
`A9066E784F6566606975CE7347CDEBBC9D09254CA9B2634DD39DE871A3DC3958`;
the full report self-hash is
`51CF54171B842797D82175A6398E5FED69FEE8B917B94887A57E7C912525E0ED`.
No Cyrillic projection was written.

All live `právě`, `działa`, and `użyvanjem` occurrences are resolved. One
explicit linguistic hold remains: `zürihsko` at cumulative base line 24,190,
whose exact line is 1,149 bytes /
`8198C93EBA16D75372D77E60B67818A5C3DC3DC37D640D8B513DDD0C87A82E62`.
The city-specific Zurich adjective is not silently normalized without adequate
authority.

## Registry validation deadlock and recovery boundary

The workspace canon registry was valid at the exact sealed EDIT-0150 head when
this transaction began. After the one authorized source apply and before the
metadata seal, the mandatory bounded validator now fails exactly at the first
registered Interslavic source pin: it expects the sealed EDIT-0150 base of
1,892,316 bytes and observes the independently accepted EDIT-0151 base of
1,892,324 bytes. This is an expected source-ahead-of-metadata transaction
midpoint, not authority for another edit.

The only permitted recovery action is to complete or roll back this already
accepted transaction. This sealer therefore binds both the exact registered
EDIT-0150 metadata graph and the exact accepted EDIT-0151 live source graph. It
must fail closed on any other registry error, source drift, or metadata drift.
No new linguistic tranche may begin from this midpoint.

The next immutable registry snapshot and handoff should state a narrow recovery
exception: when a change began from a validator-PASS registered head, has an
exact independently accepted pre-apply plan, was applied once, and has an exact
independently accepted post-apply guard, a bounded sealer or exact rollback may
complete that transaction despite the expected old-source-pin validator
failure. The exception must name the decision, predecessor and successor pins,
and may not authorize new source editing, publication, or unrelated metadata
changes.

## Rejected metadata candidate and append-only repair

The first EDIT-0151 metadata-sealer candidate, 105,155 bytes / SHA-256
`C947EA54405102521DA2D5B957F1ADA5EFC91DA7A6F3AF8DC51A9900908F84D7`,
was independently rejected before metadata apply. It was never authorized or
run in either production mode. Its transaction custody, rollback, cross-token
residue, cross-module identity, lock-last cleanup, exception-honesty, and
state/verifier graph tests passed, but its registry-completion proof did not.

That candidate changed the projected blocked union from 1,118 to 1,108 while
leaving the registry's lexical role count at 11. The four disjoint role counts
therefore still totalled 1,118, which the immutable registry validator must
reject. Its shadow also retained the `CANON-REGISTRY-0001` identifier and
snapshot address. Because the immutable v1 validator addresses the exact
`CANON-REGISTRY-0001` receipt, the shadow did not prove that a new `0002`
snapshot and receipt could be added without overwriting preserved `0001`
artifacts.

The first repair was also independently rejected before metadata apply. Its
sealer was 124,463 bytes / SHA-256
`614A5435D8CE04C3B5666B608099F3846ED6545C2D9ECA2F389BAB329A5F8903`;
its new wrapper was 7,162 bytes / SHA-256
`11CB7A898E37EC9D8CA905A883314197BF2DA3C320DDF4F4DC1DB227A3BAA679`.
Neither production mode was run. That repair correctly derived the `0002`
receipt address, corrected the lexical role count to 1, exercised the actual
validator in a complete temporary workspace, and preserved the `0001`
artifacts. It nevertheless constrained only the two validator roles itself.
The immutable core validates artifact pins that are present, but does not
require a complete successor receipt role set. It also checks the snapshot
filename rather than the complete snapshot directory address and does not pin
the schema bytes. A receipt could therefore omit or substitute infrastructure,
use a same-basename snapshot in a rogue directory, or bless a weakened schema.
The sealer also trusted wrapper PASS without independently reconstructing the
exact receipt coverage.

The second repair preserves the original validator, immutable `0001` snapshot,
`0001` receipt, both rejected candidates, and their review chronology. The
successor-aware wrapper now requires the exact snapshot path
`00_governance/canon_registry/snapshots/{registry_id}.json`, the immutable schema
pin, and exactly one exact receipt row—without omissions, duplicates, unknown
roles, or substitutions—for CURRENT, the matching snapshot, schema, handoff,
immutable v1 validator, successor-aware wrapper, preserved `0001` snapshot,
preserved `0001` receipt, and `AGENTS.md`. It strictly rejects malformed or
duplicate-key receipt JSON. The sealer reconstructs and checks the same coverage
independently after wrapper PASS, binds the exact derived receipt bytes captured
before the wrapper run to the bytes re-read afterward, and rejects a
pass-then-swap receipt replacement.

The midpoint remains governed only by the original validator. Successor
validation sets the lexical role count to 1 so the four roles total the 1,108
blocked union and runs the actual wrapper against complete disposable `0002`
and future `0003` snapshots and receipts. The shadow requires rejection of a
same-basename rogue directory, every required-role omission, duplication,
wrong path/byte count/hash, unknown-role substitution, schema weakening,
malformed JSON, duplicate-key JSON, and the earlier role/address hostiles. These
proofs also include an actual wrapper-PASS followed by a hostile receipt swap.
They write only to operating-system temporary directories removed before success;
they do not modify the live registry.

## Production-failed candidate and phase-aware repair

The next frozen sealer candidate was 142,638 bytes / SHA-256
`B4A191ABE5A9E633A88C8F71312DD8AAC510191D79D8461B6960CBBB7482DEAC`;
its human companion was 14,546 bytes / SHA-256
`18009F5D09D92821A63B2CE9778BB1963356200F3C422F928F2D34F062CC4B90`.
The successor-aware registry wrapper remained 12,599 bytes / SHA-256
`A5BC8915A61F087A98883B4C0547679C7DE726D33D2B84AAF74B3FE060FEDC1E`.
That sealer is invalidated after two identical production apply attempts failed
closed. No worklog append was attempted.

Each failed attempt exited 4 with empty stderr and a 635-byte stdout whose
SHA-256 was
`A3459B81C721E9C563730E92799DC517DDACC1C6BF81C194965073EF5B415EDA`.
The error was a `RuntimeError`: the primary rejection reported registry-midpoint
stdout drift to 226 bytes / SHA-256
`544A80E60F225C9A85243036D4D91A63331DAEB85B967F15DF0432D5420999D0`;
the custody failure state was `VALIDATING`, the terminal state was
`CLEAN_WITH_PRIMARY_ERROR`, and diagnostics were empty. Both attempts restored
the exact EDIT-0150 state, 150-row ledger, 43-row worklog, and verifier; the
EDIT-0151 sidecar was absent; registry `CURRENT` remained the exact immutable
`0001` bytes; and bounded root plus decision-record transaction residue was zero.

The defect was a phase-insensitive immutable-validator expectation. With the
accepted EDIT-0151 sources and predecessor metadata, the exact failure is the
230-byte source-pin mismatch. Once the planned successor metadata is installed
in flight, the exact immutable validator instead reports a 226-byte registered
state-pin mismatch: expected 19,786 bytes, observed 20,780. The 226-byte result
is not sufficient proof of the successor: even an arbitrary same-length
20,780-byte state produces the same output.

An intermediate phase-aware working draft was then independently rejected before
freezing or production use. It treated a caller-authored, self-consistent receipt
dictionary as if it proved the planned graph. A coherent same-length wrong
sidecar, state, ledger, worklog, and verifier set could satisfy that narrow helper
while the later deterministic snapshot validation correctly rejected it. Its
temporary baseline also checked custody before rebinding to the temporary lane,
and most negative cases exercised helpers rather than the integrated live gate.

The repaired phase boundary therefore never treats the 226-byte output or a
caller-authored evidence dictionary as proof. The live gate accepts only a pinned
in-process registry-free auxiliary capability constructed from the exact frozen
files. It executes custody on the active lane, reads the fixed metadata paths,
and runs the complete deterministic snapshot validation against the planned state
canonical digest before issuing an internal validated-graph capability. That
validation reconstructs the exact sidecar, ledger row, anticipated worklog row,
state semantics, source cohort, sealer and normalized verifier relationships.
Every graph component is then re-pinned immediately before and after the actual
immutable-validator subprocess; only after those ordered checks may the exact
226-byte result classify the phase as `REGISTRY_UPDATE_PENDING`. The 230-byte
result remains exclusive to the exact post-source/pre-metadata recovery phase.

The semantic shadow runs the actual immutable validator in a complete temporary
predecessor-registry workspace against both vectors and drives its negative cases
through the integrated gate. It requires rejection of valid arbitrary
same-length JSON state, partial or mixed metadata, missing or changed fixed-path
components, the coherent same-length wrong-metadata set described above, a change
between full validation and subprocess return, wrong-phase 230/226 outputs, and
altered exit, stderr, bytes, or line framing. Exact pending and appended worklog
graphs both pass. Custody is executed only after the temporary lane is active,
and the temporary workspace is removed before success.

## Metadata seal protocol and claim boundary

The sealer defaults to a strict no-write plan. It delegates guarded metadata
custody to the exact sealed EDIT-0150 phase-aware transaction implementation.
The first separately authorized phase may install only the verifier, previously
absent EDIT-0151 JSON sidecar, ledger row 151, and successor state while
retaining the exact 43-row worklog prefix. This human record, applicator,
sealer, scanner, registry files, source authority, and all historical records
are immutable preconditions.

Only after two actual pending verifier passes may a second separately
authorized guarded phase append the exact worklog event. Two actual final
verifier passes are then required. The source applicator's exact successor-state
invalidation is accepted only after the complete successor graph validates.
The canon registry must remain at the exact pinned EDIT-0150 snapshot throughout
metadata installation, pending verification, worklog append, both final verifier
runs, and the immediate post-seal audit. At that point the verifier reports
`REGISTRY_UPDATE_PENDING`; only then may a new immutable EDIT-0151 registry
snapshot, byte-identical `CURRENT.json`, validation receipt, and updated handoff
advance the registry. A later audit accepts that phase only when the bounded
validator passes and the registry binds this exact head, source cohort, scanner
census, and disclosed source-ahead-of-metadata recovery exception.
The protocol is cooperative same-host custody; it does not claim
source-plus-metadata atomicity, cross-crash atomicity, or serialization against
uncooperative writers.

EDIT-0151 does not freeze the Latin edition, create a Cyrillic artifact, build,
render, publish, alter a repository or DOI, claim native/community/official
certification, or broaden rights claims. The remaining `zürihsko` hold and
blocked projection census are explicit. Continue only after the metadata seal,
registry successor snapshot, and independent validation are complete.
